import configuration from './configuration';
import electron from './electron';
import history from './history';
import metadata from './metadata';

export default [electron, metadata, configuration, history];
